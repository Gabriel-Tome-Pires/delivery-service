apiVersion: v1
kind: Service
metadata:
  name: {{ include "delivery-service-chart.fullname" . }}
  labels:
    {{- include "delivery-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "delivery-service-chart.selectorLabels" . | nindent 4 }}
